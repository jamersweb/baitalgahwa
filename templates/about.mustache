{{!
    @template theme_baitalgahwa/about
    Public About page matching the supplied Figma visual.
}}
{{> theme_boost/head }}
<style>
body.bag-about-body {
    background: #fffaf3;
}
body.bag-about-body #page-header,
body.bag-about-body .secondary-navigation {
    display: none;
}
body.bag-about-body #page,
body.bag-about-body #topofscroll,
body.bag-about-body .main-inner {
    margin-top: 0 !important;
    padding-top: 0 !important;
    background: #fffaf3;
}
body.bag-about-body #page.drawers {
    padding-top: 0 !important;
}
.bag-about-page {
    --bag-brown: #7a4a22;
    --bag-gold: #c99d42;
    --bag-ink: #15110d;
    --bag-muted: #8d7f73;
    --bag-cream: #fffaf3;
    --bag-line: #eadfce;
    background: var(--bag-cream);
    color: var(--bag-ink);
    padding-top: 0;
    overflow: hidden;
}
.bag-about-section {
    width: min(100%, 1280px);
    margin: 0 auto;
    padding-inline: 0;
}
.bag-about-hero {
    position: relative;
    width: 100vw;
    max-width: 100vw;
    min-height: clamp(20rem, 25vw, 22.1rem);
    margin-inline: calc(50% - 50vw);
    display: grid;
    place-items: center;
    color: #fff;
    text-align: center;
    isolation: isolate;
    overflow: hidden;
}
.bag-about-hero::before,
.bag-about-hero::after {
    content: "";
    position: absolute;
    inset: 0;
    z-index: -2;
}
.bag-about-hero::before {
    background: url('{{{ about_hero_url }}}') center top / cover no-repeat;
    filter: grayscale(1) contrast(1.04) brightness(.7);
    transform: scale(1.015);
}
.bag-about-hero::after {
    z-index: -1;
    background: linear-gradient(180deg, rgba(15, 13, 12, .58), rgba(15, 13, 12, .7));
}
.bag-about-hero__title {
    max-width: 38rem;
    margin: 0 auto .7rem;
    color: #fff;
    font-size: clamp(1.55rem, 3.8vw, 3rem);
    line-height: 1.05;
    font-weight: 800;
    text-transform: uppercase;
}
.bag-about-hero__copy {
    max-width: 30rem;
    margin: 0 auto 1rem;
    color: rgba(255,255,255,.9);
    font-size: .9rem;
    line-height: 1.55;
}
.bag-about-btn {
    display: inline-flex;
    min-height: 2.22rem;
    align-items: center;
    justify-content: center;
    padding: .62rem 1.12rem;
    border: 1px solid var(--bag-brown);
    border-radius: .2rem;
    background: var(--bag-brown);
    color: #fff;
    font-size: .72rem;
    font-weight: 800;
    line-height: 1;
    text-decoration: none;
    box-shadow: 0 .45rem 1rem rgba(90, 52, 20, .18);
}
.bag-about-btn:hover,
.bag-about-btn:focus-visible {
    color: #fff;
    background: #5f3616;
    border-color: #5f3616;
    text-decoration: none;
}
.bag-about-expertise {
    position: relative;
    z-index: 1;
    margin-top: -24px;
}
.bag-about-panel {
    display: grid;
    grid-template-columns: 589px 559px;
    gap: 92px;
    align-items: center;
    background: #fff;
    border: 0;
    box-shadow: 0 1rem 2.4rem rgba(52, 30, 12, .08);
    padding: 80px 64px 78px;
}
.bag-about-collage {
    position: relative;
    min-height: 284px;
}
.bag-about-collage::before,
.bag-about-collage::after {
    content: "";
    position: absolute;
    border: 1px solid #e7d8c8;
}
.bag-about-collage::before {
    width: 94px;
    height: 168px;
    left: -12px;
    top: 12px;
}
.bag-about-collage::after {
    width: 119px;
    height: 93px;
    right: 8px;
    bottom: 28px;
}
.bag-about-collage__image {
    position: absolute;
    object-fit: cover;
    box-shadow: 0 .8rem 1.7rem rgba(40, 24, 12, .16);
}
.bag-about-collage__image--one {
    width: 360px;
    height: 132px;
    left: 28px;
    top: 34px;
    border-radius: 999px;
}
.bag-about-collage__image--two {
    width: 264px;
    height: 126px;
    right: 34px;
    top: 0;
}
.bag-about-collage__image--three {
    width: 352px;
    height: 127px;
    right: 97px;
    bottom: 0;
}
.bag-about-kicker {
    margin: 0 0 .5rem;
    color: var(--bag-brown);
    font-size: .75rem;
    font-weight: 800;
    text-transform: uppercase;
}
.bag-about-title {
    margin: 0;
    color: #17120f;
    font-size: clamp(2rem, 2.6vw, 39px);
    line-height: 1.12;
    font-weight: 800;
}
.bag-about-copy {
    margin: 22px 0 0;
    color: var(--bag-muted);
    font-size: 18px;
    line-height: 1.55;
}
.bag-about-metrics {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1.6rem;
    margin-top: 30px;
}
.bag-about-metric strong {
    display: block;
    color: var(--bag-gold);
    font-size: 30px;
    line-height: 1;
    font-weight: 800;
}
.bag-about-metric span {
    display: block;
    margin-top: .3rem;
    color: #6d5c4c;
    font-size: 14px;
    font-weight: 700;
}
.bag-about-development {
    padding-block: 77px 76px;
}
.bag-about-development__inner {
    display: grid;
    grid-template-columns: 585px 621px;
    gap: 74px;
    align-items: center;
}
.bag-about-checks {
    display: grid;
    gap: 16px;
    margin: 26px 0 0;
    padding: 0;
    list-style: none;
    color: #75685d;
    font-size: 16px;
}
.bag-about-checks li {
    display: flex;
    gap: .52rem;
    align-items: flex-start;
}
.bag-about-checks i {
    color: var(--bag-brown);
    margin-top: .18rem;
}
.bag-about-development__image {
    display: block;
    width: 100%;
    height: 414px;
    object-fit: cover;
}
.bag-about-cta {
    padding-bottom: 64px;
}
.bag-about-cta__panel {
    display: flex;
    flex-direction: column;
    align-items: center;
    min-height: 193px;
    gap: 16px;
    text-align: center;
    background: var(--bag-brown);
    color: #fff;
    border-radius: 6px;
    padding: 53px 1rem 41px;
}
.bag-about-cta__title {
    margin: 0;
    color: #fff;
    font-size: clamp(1.8rem, 2.6vw, 35px);
    font-weight: 800;
    line-height: 1.1;
    text-transform: uppercase;
}
.bag-about-cta .bag-about-btn {
    background: #fff4e7;
    color: var(--bag-brown);
    border-color: #fff4e7;
}
.bag-about-cta .bag-about-btn:hover,
.bag-about-cta .bag-about-btn:focus-visible {
    color: #5f3616;
    background: #fff;
    border-color: #fff;
}
@media (max-width: 991.98px) {
    .bag-about-panel,
    .bag-about-development__inner {
        grid-template-columns: 1fr;
    }
    .bag-about-section {
        padding-inline: .9rem;
    }
    .bag-about-panel {
        gap: 2rem;
    }
    .bag-about-development__image {
        height: auto;
        max-height: 28rem;
    }
}
@media (max-width: 575.98px) {
    .bag-about-page {
        padding-top: 0;
    }
    .bag-about-hero {
        min-height: 15rem;
    }
    .bag-about-hero::before {
        background-size: cover;
        background-position: center top;
    }
    .bag-about-expertise {
        margin-top: 0;
    }
    .bag-about-panel {
        padding: 1.2rem;
    }
    .bag-about-collage {
        min-height: 11.5rem;
    }
    .bag-about-metrics {
        grid-template-columns: 1fr;
    }
}
</style>
<body {{{ bodyattributes }}}>
{{> core/local/toast/wrapper}}
<div id="page-wrapper" class="d-print-block">
    {{{ output.standard_top_of_body_html }}}
    {{> theme_baitalgahwa/navbar }}
    <div id="page" data-region="mainpage" class="drawers drag-container">
        <div id="topofscroll" class="main-inner">
            <main class="bag-about-page" id="bag-about-main">
                <section class="bag-about-hero" aria-labelledby="bag-about-hero-title">
                    <div>
                        <h1 class="bag-about-hero__title" id="bag-about-hero-title">About Us</h1>
                        <p class="bag-about-hero__copy">A cultural training experience shaped around heritage, hospitality, and professional excellence.</p>
                        <a class="bag-about-btn" href="{{{ config.trainingurl }}}">Explore Programmes</a>
                    </div>
                </section>

                <section class="bag-about-expertise bag-about-section" aria-labelledby="bag-about-expertise-title">
                    <div class="bag-about-panel">
                        <div class="bag-about-collage" aria-hidden="true">
                            <img class="bag-about-collage__image bag-about-collage__image--one" src="{{{ about_coffee_url }}}" alt="" loading="lazy">
                            <img class="bag-about-collage__image bag-about-collage__image--two" src="{{{ about_collage_secondary_url }}}" alt="" loading="lazy">
                            <img class="bag-about-collage__image bag-about-collage__image--three" src="{{{ about_activity_url }}}" alt="" loading="lazy">
                        </div>
                        <div>
                            <p class="bag-about-kicker">Expertise Across All Disciplines</p>
                            <h2 class="bag-about-title" id="bag-about-expertise-title">A Training Platform Built Around Excellence</h2>
                            <p class="bag-about-copy">Bait Al Gahwa helps professionals build practical capability through guided programmes, thoughtful cultural context, and measurable learning journeys.</p>
                            <div class="bag-about-metrics" role="list">
                                <span class="bag-about-metric" role="listitem"><strong>500+</strong><span>Certified Learners</span></span>
                                <span class="bag-about-metric" role="listitem"><strong>20+</strong><span>Training Experts</span></span>
                                <span class="bag-about-metric" role="listitem"><strong>50+</strong><span>Expert Sessions</span></span>
                            </div>
                        </div>
                    </div>
                </section>

                <section class="bag-about-development">
                    <div class="bag-about-section bag-about-development__inner">
                        <div>
                            <p class="bag-about-kicker">Excellence In Professional Development</p>
                            <h2 class="bag-about-title">Developing Confident Service Leaders</h2>
                            <p class="bag-about-copy">Our learning experience brings structure, mentor guidance, and practical application together for learners and teams.</p>
                            <ul class="bag-about-checks">
                                <li><i class="fa fa-check-circle-o" aria-hidden="true"></i><span>Programmes aligned with professional growth and workplace outcomes.</span></li>
                                <li><i class="fa fa-check-circle-o" aria-hidden="true"></i><span>Hands-on training supported by experienced specialists.</span></li>
                                <li><i class="fa fa-check-circle-o" aria-hidden="true"></i><span>Progress-focused learning paths for individuals and organizations.</span></li>
                            </ul>
                        </div>
                        <div>
                            <img class="bag-about-development__image" src="{{{ about_member_url }}}" alt="" loading="lazy">
                        </div>
                    </div>
                </section>

                <section class="bag-about-cta bag-about-section">
                    <div class="bag-about-cta__panel">
                        <h2 class="bag-about-cta__title">Ready To Begin Your Journey?</h2>
                        <a class="bag-about-btn" href="{{{ config.trainingurl }}}">Start Training</a>
                    </div>
                </section>
            </main>
        </div>
        {{> theme_baitalgahwa/footer }}
    </div>
    {{{ output.standard_after_main_region_html }}}
</div>
</body>
</html>
{{#js}}
M.util.js_pending('theme_boost/loader');
require(['theme_boost/loader', 'theme_boost/drawer', 'theme_baitalgahwa/theme'], function(Loader, Drawer, Bait) {
    Drawer.init();
    Bait.init();
    M.util.js_complete('theme_boost/loader');
});
{{/js}}
